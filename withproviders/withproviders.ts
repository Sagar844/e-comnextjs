import { useContext } from "react";

// const withprovider =
//   (provider: any) => (IncomingComponent: any) => (props: any) => {
//     const contextData = useContext(provider);
//     return <IncomingComponent {...props} {...contextData} />;
//   };
//   import React, { useContext } from "react";
